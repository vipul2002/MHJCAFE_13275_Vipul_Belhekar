// const slides = document.getElementsByClassName('.slides1');
// const items = document.getElementsByClassName('.item1');
// const itemWidth = items[0].getBoundingClientRect().width;

// let position = 0;
// slides.innerHTML += slides.innerHTML;
// function autoScroll() {
//     position -= itemWidth /30; // Adjust speed by changing divisor
//     if (Math.abs(position) >= slides.scrollWidth / 2) {
//         position = 0; // Reset position for looping
//     }
//     slides.style.transform = `translateX(${position}px)`;
//     requestAnimationFrame(autoScroll);
// }

// // Duplicate items for seamless scrolling
// // slides.innerHTML += slides.innerHTML;

// autoScroll();

